{
  "name": "playwright-issue",
  "version": "1.0.0",
  "description": "hardware acceleration issue for playwright",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.28.0"
  }
}
